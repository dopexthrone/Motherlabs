{
  "name": "motherlabs-runtime",
  "version": "0.1.0",
  "description": "Motherlabs Runtime - Deterministic Reasoning Engine",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "npm run build && npx tsx tests/urco.test.ts && npx tsx tests/urco-failures.test.ts && npx tsx tests/llm-deterministic.test.ts && npx tsx tests/state-corruption.test.ts && npx tsx tests/serialization.test.ts",
    "test:urco": "npm run build && npx tsx tests/urco.test.ts",
    "test:failures": "npm run build && npx tsx tests/urco-failures.test.ts",
    "test:deterministic": "npm run build && npx tsx tests/llm-deterministic.test.ts",
    "benchmark": "npm run build && node dist/benchmark/cli.js",
    "benchmark:warmup": "npm run benchmark -- --warmup",
    "benchmark:full": "npm run benchmark -- --full",
    "benchmark:traps": "npm run build && node dist/benchmark/trap-cli.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "openai": "^6.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-morph": "^27.0.2",
    "typescript": "^5.3.0"
  }
}
