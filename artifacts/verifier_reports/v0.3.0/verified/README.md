# Verified Reports (v0.3.0)

Submissions that passed verification (summary.result == PASS).

Each folder follows the naming convention: `<YYYYMMDD>_<verifier_id>/`

These reports are indexed in `../../INDEX.md`.
